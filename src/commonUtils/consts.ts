export const AccessTokenStorageKey = "rlife.access_token";
export const RefreshTokenStorageKey = "rlife.refresh_token";

export const SignupPageUrl = window.location.href.split("/")[0] + "/signup";

export const TaskPageUrl = window.location.href.split("/")[0] + "/tasks";

export const ExpensePageUrl = window.location.href.split("/")[0] + "/expenses";