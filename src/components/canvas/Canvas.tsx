import { observer } from 'mobx-react';
import React from 'react';

@observer
export class Canvas extends React.Component {
  render() {
    return (
      <div>
        CANVAS PAGE
      </div>
    )
  }
}