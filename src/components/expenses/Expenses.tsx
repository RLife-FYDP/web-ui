import { observer } from 'mobx-react';
import React from 'react';

@observer
export class Expenses extends React.Component {
  render() {
    return (
      <div>
        EXPENSES PAGE
      </div>
    )
  }
}